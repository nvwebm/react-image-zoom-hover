import * as React from 'react';
interface Props {
    children: React.ReactHTMLElement<HTMLImageElement>;
    bigImage: string;
}
export declare const ImageZoom: ({ children, bigImage }: Props) => JSX.Element;
export {};
